# Claude Code local settings
.claude/

# Editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Ansible retry files
*.retry

# Any local overrides (use these if you want to keep personal values out of the repo)
ansible/inventory.local.yaml
*.local.yaml
*.local.yml

# Generated Makefile vars (derived from group_vars/all.yaml)
.make-vars

# Kubeconfig (written to ~/.kube/, but just in case)
kubeconfig
*.kubeconfig

# Local session state (not for version control)
STATE.md
