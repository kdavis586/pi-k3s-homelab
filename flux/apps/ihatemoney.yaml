apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ihatemoney
  namespace: flux-system
spec:
  interval: 10m
  targetNamespace: ihatemoney
  chart:
    spec:
      chart: ./charts/ihatemoney
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      reconcileStrategy: Revision
  upgrade:
    cleanupOnFail: true
