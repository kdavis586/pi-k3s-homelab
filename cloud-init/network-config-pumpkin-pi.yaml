# Generated by: make generate  (source: ansible/group_vars/all.yaml)
# DIAGNOSTIC: wildcard match + DHCP to identify real interface name on boot
version: 2
ethernets:
  pi-eth:
    match:
      name: "e*"
    dhcp4: true
