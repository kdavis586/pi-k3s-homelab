---
phase: 04-makefile-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - ansible/playbooks/deploy.yaml
  - CLAUDE.md
  - README.md
  - SETUP.md
autonomous: true
requirements: [MAKE-01, MAKE-02, MAKE-03]

must_haves:
  truths:
    - "Running `make deploy` produces 'No rule to make target' error (target fully removed)"
    - "ansible/playbooks/deploy.yaml does not exist on disk"
    - "make flux-status is documented as the diagnostic command in CLAUDE.md"
    - "No .md file in the repo root references `make deploy` as a current workflow command"
    - "CLAUDE.md contains `flux get` diagnostic commands"
  artifacts:
    - path: "Makefile"
      provides: "Clean Makefile without deploy target"
      contains: "flux-status"
    - path: "CLAUDE.md"
      provides: "Updated workflow docs with GitOps path"
      contains: "make flux-status"
    - path: "README.md"
      provides: "Updated cluster bring-up instructions"
      contains: "make bootstrap-flux"
    - path: "SETUP.md"
      provides: "Updated Step 5 with bootstrap-flux"
      contains: "make bootstrap-flux"
  key_links:
    - from: "Makefile"
      to: "CLAUDE.md"
      via: "Documented targets match actual targets"
      pattern: "make flux-status"
    - from: "Makefile"
      to: "SETUP.md"
      via: "Step 5 bring-up sequence matches available targets"
      pattern: "make bootstrap-flux"
---

<objective>
Remove the imperative `make deploy` path and update all documentation to reflect GitOps as the sole deploy mechanism.

Purpose: After Phase 3 completed the Flux ownership transfer and deleted `k8s/`, the `deploy` Makefile target and its backing playbook are dead code. Leaving them creates confusion about how deployments work. This plan removes the dead code and updates CLAUDE.md, README.md, and SETUP.md to document the GitOps workflow (push to main, Flux reconciles).

Output: Clean Makefile, deleted deploy.yaml, updated docs -- zero references to `make deploy` as a current workflow.
</objective>

<execution_context>
@/Users/KaelanDavis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/KaelanDavis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-makefile-cleanup/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deploy target from Makefile and delete deploy.yaml</name>
  <files>Makefile, ansible/playbooks/deploy.yaml</files>
  <read_first>Makefile, ansible/playbooks/deploy.yaml</read_first>
  <action>
1. Edit `Makefile`:
   - Remove `deploy` from the `.PHONY` line (line 13). The new `.PHONY` line should be:
     ```
     .PHONY: help generate setup install-k3s status logs bootstrap-flux flux-status flux-reconcile
     ```
   - Delete the entire `deploy` target block (lines 33-34):
     ```
     deploy: ## Apply all k8s/ manifests to the cluster (use before Flux is bootstrapped)
     	$(ANSIBLE) ansible/playbooks/deploy.yaml
     ```
   - Keep the `# -- Day-to-day` section header comment (line 31) but remove it if the section becomes empty. Since `deploy` was the only target under "Day-to-day", remove the entire section header and blank line (lines 31-34). The Makefile should flow directly from the `install-k3s` target to the `# -- Flux CD` section.

2. Delete `ansible/playbooks/deploy.yaml` entirely (rm the file).

No other Makefile targets are modified. `flux-status`, `flux-reconcile`, `bootstrap-flux`, `status`, `generate`, `setup`, `install-k3s`, `logs`, `ssh-%` all remain unchanged.
  </action>
  <verify>
    <automated>cd /Users/KaelanDavis/Documents/Projects/coding/pi-k3s-homelab && make deploy 2>&1 | grep -q "No rule to make target" && echo "PASS: deploy target removed" || echo "FAIL: deploy target still exists"; test ! -f ansible/playbooks/deploy.yaml && echo "PASS: deploy.yaml deleted" || echo "FAIL: deploy.yaml still exists"; make help 2>&1 | grep -v deploy | head -1 > /dev/null && echo "PASS: make help runs" || echo "FAIL: make help broken"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "deploy" Makefile` returns 0 (no occurrence of the word "deploy" in the Makefile -- the target, .PHONY entry, and section are all gone)
    - `test ! -f ansible/playbooks/deploy.yaml` exits 0
    - `make help` runs without error and its output does NOT contain the word "deploy"
    - `make help` output DOES contain "flux-status", "flux-reconcile", "bootstrap-flux"
    - The Makefile still has exactly the same number of working targets minus one (deploy removed)
  </acceptance_criteria>
  <done>The deploy target is completely absent from the Makefile, deploy.yaml is deleted, and all remaining targets (including flux-status satisfying MAKE-02) work correctly via make help.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md, README.md, and SETUP.md to reflect GitOps workflow</name>
  <files>CLAUDE.md, README.md, SETUP.md</files>
  <read_first>CLAUDE.md, README.md, SETUP.md</read_first>
  <action>
1. **CLAUDE.md** -- Update the Workflow section (lines 13-19) command block. Replace the existing bash block with:
   ```bash
   make generate       # Render all Jinja2 templates -> cloud-init files, inventory
   make setup          # Ansible: base OS setup + USB mount + avahi (idempotent)
   make install-k3s    # Ansible: install K3s server then agents (idempotent)
   make bootstrap-flux # Bootstrap Flux CD onto cluster (one-time, requires bw unlocked)
   make status         # kubectl get nodes + pods + svc + pvc
   make flux-status    # Show Flux reconciliation state for all resources
   make flux-reconcile # Force immediate git sync (instead of waiting for 1-min poll)
   make ssh-<name>     # e.g. make ssh-the-bakery, make ssh-apple-pi
   ```

2. **CLAUDE.md** -- Add a new subsection after the "CRITICAL: Use make for everything" section (after line 34), before the "K3s / Ubuntu gotchas" section. Insert:
   ```markdown
   ### Deploying workloads (GitOps)

   Flux CD watches the `main` branch and reconciles within 60 seconds of a push:
   ```bash
   git add charts/jellyfin/  # or whatever changed
   git commit -m "feat: ..."
   git push origin main
   make flux-status          # watch reconciliation
   ```

   To trigger an immediate sync without waiting for the poll interval:
   ```bash
   make flux-reconcile       # force immediate sync
   make flux-status          # confirm READY=True
   ```

   Diagnostic commands (run directly, these are read-only):
   ```bash
   flux get all              # all Flux resources across namespaces
   flux get helmreleases -A  # HelmRelease status
   flux get sources git -A   # GitRepository polling status
   flux logs                 # Flux controller event stream
   ```
   ```

3. **CLAUDE.md** -- In the "CRITICAL: Use make for everything" section, update the "Wrong/Right" examples. Change:
   - Wrong line: keep `kubectl apply -f ...`, `kubectl label ...`, `ansible-playbook ...` as-is (still wrong)
   - Right line: change to `encode the change in Helm charts/values, commit, and push to main`

4. **CLAUDE.md** -- In the "Key files" table (lines 193-204):
   - Remove the row `| k8s/jellyfin/ | Jellyfin manifests |` (this directory was deleted in Phase 3)
   - Remove the row `| k8s/storage/local-path-config.yaml | Configures local-path-provisioner storage paths |` (also deleted)
   - Add these rows:
     - `| charts/jellyfin/ | Jellyfin Helm chart (deployed by Flux) |`
     - `| charts/pihole/ | Pi-hole Helm chart (deployed by Flux) |`
     - `| flux/apps/ | HelmRelease CRDs for Flux to deploy |`
     - `| flux/flux-system/ | Flux bootstrap manifests (do not edit manually) |`

5. **README.md** -- In the "Cluster bring-up" section (lines 64-69), replace the bash block with:
   ```bash
   make generate       # Render all Jinja2 templates
   make setup          # Ansible: OS prep, USB mount, avahi mDNS
   make install-k3s    # Ansible: install K3s server then agents
   make bootstrap-flux # Bootstrap Flux CD (one-time, requires bw unlocked)
   make status         # Check nodes and pods
   make flux-status    # Check Flux reconciliation state
   ```

6. **README.md** -- In the "Repo Structure" section (lines 82-100), update the tree to reflect post-Phase-3 state:
   - Remove `k8s/` section (templates/, jellyfin/, storage/)
   - Add after the cloud-init section:
     ```
     charts/
       jellyfin/                <- Jellyfin Helm chart
       pihole/                  <- Pi-hole Helm chart
     flux/
       apps/                    <- HelmRelease CRDs
       flux-system/             <- Flux bootstrap (do not edit)
     ```

7. **SETUP.md** -- In Step 5 (lines 90-93), replace the bash block with:
   ```bash
   make setup          # OS prep, USB mount on apple-pi, avahi mDNS
   make install-k3s    # Install K3s server then agents
   make bootstrap-flux # Bootstrap Flux CD (requires bw unlocked + flux CLI)
   ```
   Add a note after the block:
   ```
   After bootstrap, workloads deploy automatically when you push to `main`.
   Run `make flux-status` to check reconciliation state.
   ```
  </action>
  <verify>
    <automated>cd /Users/KaelanDavis/Documents/Projects/coding/pi-k3s-homelab && echo "--- CLAUDE.md ---" && grep -c "make deploy" CLAUDE.md && echo "FAIL" || echo "PASS: no make deploy in CLAUDE.md"; grep -c "flux get" CLAUDE.md | xargs -I{} test {} -ge 1 && echo "PASS: flux get in CLAUDE.md" || echo "FAIL"; echo "--- README.md ---" && grep -c "make deploy" README.md && echo "FAIL" || echo "PASS: no make deploy in README.md"; echo "--- SETUP.md ---" && grep -c "make deploy" SETUP.md && echo "FAIL" || echo "PASS: no make deploy in SETUP.md"; grep -c "bootstrap-flux" SETUP.md | xargs -I{} test {} -ge 1 && echo "PASS: bootstrap-flux in SETUP.md" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "make deploy" CLAUDE.md` returns 0
    - `grep -c "make deploy" README.md` returns 0
    - `grep -c "make deploy" SETUP.md` returns 0
    - `grep -c "flux get" CLAUDE.md` returns >= 4 (flux get all, flux get helmreleases, flux get sources, plus at least one more)
    - `grep -c "make flux-status" CLAUDE.md` returns >= 1
    - `grep -c "make bootstrap-flux" CLAUDE.md` returns >= 1
    - `grep -c "make bootstrap-flux" README.md` returns >= 1
    - `grep -c "make bootstrap-flux" SETUP.md` returns >= 1
    - `grep -c "charts/jellyfin" CLAUDE.md` returns >= 1 (Key files table updated)
    - `grep -c "charts/pihole" CLAUDE.md` returns >= 1 (Key files table updated)
    - `grep -c "k8s/jellyfin" CLAUDE.md` returns 0 (stale reference removed)
    - `grep -c "k8s/" README.md` returns 0 (stale repo structure removed)
    - CLAUDE.md contains the string "Deploying workloads (GitOps)" (new subsection header)
    - SETUP.md contains the string "workloads deploy automatically" (post-bootstrap note)
  </acceptance_criteria>
  <done>All three documentation files reference GitOps/Flux as the deploy path, contain no references to `make deploy` as a current workflow, CLAUDE.md has `flux get` diagnostic commands, and the Key files table and README repo structure reflect the post-Phase-3 directory layout.</done>
</task>

</tasks>

<verification>
Final phase verification after both tasks complete:

1. `grep -r "make deploy" *.md` returns zero results across all root .md files
2. `make help` output lists flux-status, flux-reconcile, bootstrap-flux but NOT deploy
3. `make deploy 2>&1 | grep "No rule"` confirms target is gone
4. `test ! -f ansible/playbooks/deploy.yaml` confirms playbook deleted
5. `grep "flux get" CLAUDE.md` returns multiple matches (diagnostic commands documented)
6. `grep "bootstrap-flux" SETUP.md README.md CLAUDE.md` returns matches in all three files
</verification>

<success_criteria>
- The `make deploy` target is completely removed from the Makefile (MAKE-01)
- `ansible/playbooks/deploy.yaml` is deleted (MAKE-01)
- `make flux-status` exists and is documented in CLAUDE.md (MAKE-02 -- target already existed, now documented)
- CLAUDE.md references `flux get` commands for diagnostics, not `kubectl apply` for deploys (MAKE-03)
- README.md and SETUP.md cluster bring-up sequences use `make bootstrap-flux` instead of `make deploy`
- Zero occurrences of `make deploy` as a current workflow in any .md file in the repo root
</success_criteria>

<output>
After completion, create `.planning/phases/04-makefile-cleanup/04-01-SUMMARY.md`
</output>
