---
phase: 02-flux-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - k8s/flux-system/gotk-sync.yaml
autonomous: true
requirements:
  - BOOT-01
  - BOOT-04
must_haves:
  truths:
    - "make bootstrap-flux uses --path=flux so Flux manifests land in flux/flux-system/"
    - "Stale k8s/flux-system/gotk-sync.yaml is deleted from the repo"
  artifacts:
    - path: "Makefile"
      provides: "Corrected bootstrap-flux target with --path=flux and --version flag"
      contains: "--path=flux"
    - path: "k8s/flux-system/gotk-sync.yaml"
      provides: "DELETED — must not exist after this plan"
  key_links:
    - from: "Makefile"
      to: "flux bootstrap github"
      via: "--path=flux flag"
      pattern: "--path=flux"
---

<objective>
Fix the Makefile bootstrap-flux target and remove the stale gotk-sync.yaml stub so bootstrap will produce the correct file layout.

Purpose: The current Makefile has --path=k8s (wrong — would create k8s/flux-system/ instead of flux/flux-system/). The stale k8s/flux-system/gotk-sync.yaml uses HTTPS URL and path: ./k8s which is dead code. Both must be fixed before running bootstrap.

Output: Corrected Makefile target, stale stub deleted.
</objective>

<execution_context>
@/Users/KaelanDavis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/KaelanDavis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-flux-bootstrap/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix bootstrap-flux Makefile target</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (current bootstrap-flux target at line 32-41)
    - ansible/group_vars/all.yaml (flux_version: v2.4.0 at line 38)
  </read_first>
  <action>
Edit the `bootstrap-flux` target in the Makefile to fix two issues:

1. Change `--path=k8s` to `--path=flux` — bootstrap creates a `flux-system/` subdirectory inside `--path`, so `--path=flux` produces `flux/flux-system/` which is where Phase 1 artifacts live.

2. Add `--version=v2.4.0` flag to pin the Flux controller version to match `flux_version` in all.yaml. This prevents the CLI from installing whatever version it ships with.

The corrected bootstrap command should be:
```
flux bootstrap github \
    --owner=kdavis586 \
    --repository=pi-k3s-homelab \
    --branch=main \
    --path=flux \
    --personal \
    --version=v2.4.0 \
    --kubeconfig=$(KUBECONFIG)
```

Do NOT change the guard checks (command -v flux, GITHUB_TOKEN check) — they are correct.
Do NOT change the help comment — it is correct.
Do NOT change any other Makefile targets.
  </action>
  <verify>
    <automated>grep -q -- '--path=flux' Makefile && grep -q -- '--version=v2.4.0' Makefile && ! grep -q -- '--path=k8s' Makefile && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `grep '--path=flux' Makefile` matches exactly one line containing `--path=flux \`
    - `grep '--path=k8s' Makefile` returns no matches (exit code 1)
    - `grep '--version=v2.4.0' Makefile` matches exactly one line
    - The guard checks `command -v flux` and `[ -n "$(GITHUB_TOKEN)" ]` are unchanged
    - All other Makefile targets are unchanged
  </acceptance_criteria>
  <done>Makefile bootstrap-flux target uses --path=flux and --version=v2.4.0; old --path=k8s is gone</done>
</task>

<task type="auto">
  <name>Task 2: Delete stale k8s/flux-system/gotk-sync.yaml</name>
  <files>k8s/flux-system/gotk-sync.yaml</files>
  <read_first>
    - k8s/flux-system/gotk-sync.yaml (the stale stub using HTTPS URL and path: ./k8s)
  </read_first>
  <action>
Delete the file `k8s/flux-system/gotk-sync.yaml` using `git rm`. This file is a pre-Phase-1 stub that:
- Uses HTTPS URL (not SSH) for the GitRepository
- Points to `path: ./k8s` instead of `./flux/flux-system`
- Has `prune: true` which would be dangerous if applied

It is dead code that will never be applied (bootstrap creates the real gotk-sync.yaml in `flux/flux-system/`), but leaving it risks confusion or accidental application via `make deploy`.

After deletion, if the `k8s/flux-system/` directory is empty, remove it too (git rm handles this automatically when the last file is removed).

Do NOT delete anything else in the `k8s/` directory — `k8s/jellyfin/` and `k8s/pihole/` must remain until Phase 4.
  </action>
  <verify>
    <automated>test ! -f k8s/flux-system/gotk-sync.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `ls k8s/flux-system/gotk-sync.yaml` returns "No such file or directory"
    - `ls k8s/jellyfin/` still lists files (jellyfin raw manifests preserved)
    - `ls k8s/pihole/` still lists files (pihole raw manifests preserved)
  </acceptance_criteria>
  <done>Stale gotk-sync.yaml deleted; k8s/jellyfin/ and k8s/pihole/ still present</done>
</task>

</tasks>

<verification>
- `grep '--path=flux' Makefile` returns a match
- `grep '--path=k8s' Makefile` returns no match
- `grep '--version=v2.4.0' Makefile` returns a match
- `k8s/flux-system/gotk-sync.yaml` does not exist
- `k8s/jellyfin/` and `k8s/pihole/` directories still exist
</verification>

<success_criteria>
Makefile bootstrap-flux target is corrected and stale stub is removed. Running `make bootstrap-flux` (with GITHUB_TOKEN set) will produce Flux manifests in flux/flux-system/ and use Flux v2.4.0 controllers.
</success_criteria>

<output>
After completion, create `.planning/phases/02-flux-bootstrap/02-01-SUMMARY.md`
</output>
