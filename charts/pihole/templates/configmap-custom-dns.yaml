apiVersion: v1
kind: ConfigMap
metadata:
  name: pihole-custom-dns
  namespace: pihole
  labels:
    {{- include "pihole.labels" . | nindent 4 }}
data:
  custom.list: |
    {{- range .Values.localDns }}
    {{ .ip }} {{ .hostname }}
    {{- end }}
