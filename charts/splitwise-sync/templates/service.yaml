apiVersion: v1
kind: Service
metadata:
  name: splitwise-sync-webapp
  namespace: splitwise-sync
  labels:
    {{- include "splitwise-sync.labels" . | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: splitwise-sync-webapp
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.webapp.port }}
